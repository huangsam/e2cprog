#include <stdio.h>
#include <stdlib.h>
int bc(int *a, int ln, int exp, char arName);
main() 
{
printf(
"%s\n", "hi"
);
if(
3
>
4
)
{
printf(
"%s\n", "not okay"
);
}
else
{
printf(
"%s\n", "this works okay"
);
printf(
"%s\n", "really it does"
);
}
printf(
"%s\n", "bye, bye for now"
);
}
